defaults:
  - annotations: new_playtable_validation
  - callbacks/rollout_lh/tasks@tasks: new_playtable_tasks

eval_cfg_overwrite:
  model:
    num_sampling_steps: ${num_sampling_steps}

train_folder: /home/troth/code/hiwi/iTRAP/iTRAP/models/flower_vla_calvin/pretrained/1000-batches_tmpdir/seed_42/saved_models/epoch=04_eval_lh
checkpoint: /home/troth/code/hiwi/iTRAP/iTRAP/models/flower_vla_calvin/pretrained/1000-batches_tmpdir/seed_42/saved_models/epoch=04_eval_lh/model_cleaned.safetensors
device: 0


log_dir: /home/troth/code/hiwi/iTRAP/outputs/
dataset_path: /DATA/calvin/task_ABC_D
num_videos: 30
debug: False

wandb:
  log: True
  project: flower_calvin_abc_eval
  group: ${now:%Y-%m-%d}
  name: ${now:%H-%M-%S}
  entity: thomas-roth-karlsruhe-institute-of-technology

num_sequences: 1000
ep_len: 360
num_sampling_steps: 4
multistep: 10

record: True
save_traj_imgs: False
